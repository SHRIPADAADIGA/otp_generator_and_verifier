# otp_generator_and_verifier
This is an alphanumeric OTP generator and verifier using python, which takes the user input email id and sends the generated OTP automatically to the user, later verifying it.
